# TP-Obj.Avanzado

El proyecto va a ser desarollado en python conectando a una base de datos PostgreSQL

Endpoints:
* register (POST)
* login (POST)
* profile (GET) [Token]
* inventory (GET) [Token]
* shop (POST) [Token]
* products (GET) [Token]
