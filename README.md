# TP-Obj.Avanzado

El proyecto va a ser desarollado en python conectando a una base de datos PostgreSQL

Endpoints:
* login (POST)
* profile (GET) [Token]
* inventory (GET) [Token]
* shop (POST) [Token]
* catalog (GET)
